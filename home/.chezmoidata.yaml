name: Gary Jarrel
email: gary@jarrel.com.au
githubUser: garyj
langs:
  python: "3.14"
  mongodb: "8.2"

# Pinned Tier-3 CLI tools. Future: templatize configs that reference these.
# See memory: feedback_install_discipline.md + project_deps_scanner_skill.md
tools:
  worktrunk: "0.38.0"      # mise+aqua, audited clean 2026-04-17
  dust: "1.2.4"            # mise+aqua (bootandy/dust)
  ralph_tui: "0.11.0"      # bun install -g (subsy/ralph-tui)

# Pinned git refs for chezmoi externals (upstream repos we pull archives from).
externals:
  zsh_syntax_highlighting: "0.8.0"   # zsh-users/zsh-syntax-highlighting (no v prefix)
  zsh_autosuggestions: "v0.7.1"      # zsh-users/zsh-autosuggestions
